name: Build ddns-go SPK

on:
  push:
    branches:
      - master
    paths:
      - 'spk/ddns-go/**'
      - '.github/workflows/build-ddns-go.yml'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g. 6.17.6). Leave empty to build the latest stable release.'
        required: false
        default: ''
      target_arch:
        description: 'Target arch (e.g. x64-7.2, x64-6.2.4, armv8-7.2, noarch-7.2)'
        required: false
        default: 'x64-7.2'
  # Periodically rebuild against the latest stable upstream release. The
  # Makefile resolves the newest version via src/resolve-versions.sh at build
  # time, so a scheduled run picks up upstream updates automatically.
  schedule:
    - cron: '0 3 * * *'   # daily at 03:00 UTC

jobs:
  build-ddns-go:
    name: Build ddns-go (${{ github.event.inputs.target_arch || 'x64-7.2' }})
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          else
            V=$(sh spk/ddns-go/src/resolve-versions.sh | sed -n 's/^SPK_VERSION=//p')
            echo "Resolved latest ddns-go version: $V"
            echo "VERSION=$V" >> "$GITHUB_OUTPUT"
          fi

      - name: Cache toolchains
        uses: actions/cache@v4
        with:
          path: toolchain/*/work
          key: toolchain-ddns-go-v1-${{ hashFiles('toolchain/*/digests') }}

      - name: Build ddns-go package
        uses: docker://ghcr.io/synocommunity/spksrc:latest
        env:
          SPK_VERS: ${{ steps.version.outputs.VERSION }}
          PKG_VERS: ${{ steps.version.outputs.VERSION }}
        with:
          entrypoint: /bin/bash
          args: -lc "set -e; cd /github/workspace/spk/ddns-go && make V=s arch-${{ github.event.inputs.target_arch || 'x64-7.2' }}"

      - name: Upload SPK artifact
        uses: actions/upload-artifact@v4
        with:
          name: ddns-go-${{ github.event.inputs.target_arch || 'x64-7.2' }}-${{ steps.version.outputs.VERSION }}
          path: packages/ddns-go_*.spk
          if-no-files-found: error

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ddns-go-${{ github.event.inputs.target_arch || 'x64-7.2' }}-logs
          path: |
            spk/ddns-go/work-*/doctor.log
            spk/ddns-go/status-build.log
            spk/ddns-go/build-*.log
          if-no-files-found: warn
